server:
  port: 8086
  error:
    include-stacktrace: never

spring:
  application:
    name: payment-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:payments
    driver-class-name: org.h2.Driver
